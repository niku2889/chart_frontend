import { Routes } from '@angular/router';

import { ResizableComponent }  from './resizable/resizable.component';

export const ResizableRoutes: Routes = [{
	path: '',
	component: ResizableComponent
}];
